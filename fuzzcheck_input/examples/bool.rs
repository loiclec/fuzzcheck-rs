// extern crate fuzzcheck_input;
// use fuzzcheck::input::FuzzedInput;
// use fuzzcheck_input::bool::*;

// type F = FuzzedBool;

fn main() {
    //     let mut x: bool = F::default();
    //     let mut x_state = F::state_from_value(&x);

    //     let mut results: Vec<bool> = vec![x];
    //     for i in 0..3 {
    //         let token = F::mutate(&mut x, &mut x_state, 1.0);
    //         results.push(x);
    //         F::unmutate(&mut x, &mut x_state, token);
    //     }
    //     println!("{:?}", results);

    //     results.clear();

    //     for i in 0..4 {
    //         results.push(F::arbitrary(i, 1.0));
    //     }

    //     println!("{:?}", results);
}
